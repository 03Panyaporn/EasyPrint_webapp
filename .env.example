# ก็อปไฟล์นี้เป็น .env แล้วใส่ค่าจริง (ห้าม commit .env ขึ้น GitHub)

# Supabase (Database + Storage)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# Auth
JWT_SECRET=

# API
PORT=3000
