# AGENTS.md — EasyPrint (Copy Shop Management System)

> **ไฟล์นี้คืออะไร:** เป็นไฟล์ที่ AI agent (Antigravity, Claude Code) จะอ่านอัตโนมัติทุกครั้งที่เปิดโปรเจกต์นี้ เพื่อให้เข้าใจบริบททั้งหมดโดยไม่ต้องอธิบายซ้ำทุกครั้งที่คุยกับมัน
>
> **ทำไมต้องมีไฟล์นี้:** ถ้าไม่มี ทุกครั้งที่เปิด session ใหม่ AI จะไม่รู้ว่าโปรเจกต์นี้คืออะไร ใช้ tech อะไร มีกติกาอะไรบ้าง ต้องพิมพ์อธิบายใหม่ทุกรอบ — ไฟล์นี้แก้ปัญหานั้นโดยให้ AI "อ่านการ์ดสรุป" ก่อนเริ่มงานเสมอ
>
> รายละเอียดเต็มของข้อเสนอโครงการอยู่ที่ `docs/proposal.md`

---

## 1. โปรเจกต์นี้คืออะไร

**EasyPrint** คือเว็บแอปสำหรับร้านถ่ายเอกสาร ที่ทำให้ลูกค้าสั่งพิมพ์งานออนไลน์ได้ โดยไม่ต้องเดินทางไปต่อคิวที่ร้าน ระบบมีผู้ใช้งาน 3 กลุ่ม:

- **ร้านถ่ายเอกสาร (Shop Owners)** — เจ้าของร้าน จัดการคำสั่งซื้อ ตั้งราคา ดูรายงานรายได้
- **ผู้ใช้บริการ (Customers)** — ลูกค้า อัปโหลดไฟล์ สั่งพิมพ์ ติดตามสถานะงาน
- **ผู้ดูแลระบบ (Admin)** — ดูแลระบบทั้งหมด อนุมัติ/ระงับบัญชีร้านค้า

รายละเอียด scope เต็มดูที่ `docs/proposal.md` หัวข้อ 1.3

---

## 2. เครื่องมือที่ใช้พัฒนา (สำคัญ อ่านก่อนเริ่ม)

โปรเจกต์นี้ใช้ **AI coding agent** ช่วยเขียนโค้ด แทนการพิมพ์เองทั้งหมด เหตุผลคือช่วยประหยัดเวลาและลดข้อผิดพลาดซ้ำๆ (เช่น validate ข้อมูล, เขียน boilerplate code)

### เครื่องมือหลัก: **Antigravity แอป** (ของ Google)

**ทำไมเลือกตัวนี้เป็นหลัก:** เพราะเป็น IDE (โปรแกรมแก้โค้ด) ที่สร้างมาคู่กับ AI agent โดยตรง เปิดแอปเดียวได้ทั้งดูไฟล์ แก้โค้ด และคุยกับ AI ในหน้าต่างเดียวกัน ไม่ต้องสลับแอปไปมา

**วิธีเปิดใช้งาน:**
1. เปิดแอป Antigravity
2. เปิดโฟลเดอร์โปรเจกต์ (`D:\easyprint`)
3. เปิด terminal ในตัวแอป (มี panel terminal อยู่ในตัวอยู่แล้ว)
4. พิมพ์ `agy` เพื่อเริ่มคุยกับ AI

**สิ่งที่จะเห็น:** พอ AI แก้ไฟล์ ไฟล์ที่เปิดอยู่ในแอปจะอัปเดตให้เห็นทันที มีสีไฮไลต์บอกว่าบรรทัดไหนถูกเพิ่ม/ลบ (คล้าย Track Changes ใน Word)

### เครื่องมือสำรอง: **Claude Code**

**ทำไมมีตัวสำรอง:** เผื่อ Antigravity (ใช้โมเดล Gemini เป็นหลัก) token/quota หมดในวันนั้น จะได้สลับมาใช้ Claude Code ต่อได้ทันทีโดยไม่เสียเวลา เพราะอ่านไฟล์ `AGENTS.md` นี้ได้เหมือนกัน (ทำงานต่อจากจุดเดิมได้)

**วิธีเปิดใช้งาน:** พิมพ์ `claude` แทน `agy` ใน terminal เดียวกัน

### กติกาการพิมพ์คำสั่งกับ AI
- พิมพ์คำสั่งงานต่อท้าย prompt (`>`) **ในหน้าต่าง terminal ที่เปิด `agy` หรือ `claude` ค้างไว้เท่านั้น**
- ต้องอยู่ใน folder โปรเจกต์ (`D:\easyprint`) ก่อนพิมพ์ `agy`/`claude` เสมอ ไม่งั้น AI จะไม่เห็นไฟล์ในโปรเจกต์
- ถ้าจะสลับจาก `agy` ไป `claude` (หรือกลับกัน) ให้กด `Ctrl+C` หรือพิมพ์ `exit` ออกจากตัวเดิมก่อน แล้วค่อยพิมพ์คำสั่งเปิดตัวใหม่

---

## 3. โครงสร้างโปรเจกต์ (Monorepo)

**Monorepo แปลว่าอะไร:** คือการเก็บทั้ง frontend (หน้าเว็บที่ลูกค้าเห็น) และ backend (ระบบหลังบ้านที่จัดการข้อมูล) ไว้ใน repo (โฟลเดอร์โค้ด) เดียวกัน แทนที่จะแยกเป็น 2 repo — ทำให้แก้ไฟล์ที่เกี่ยวข้องกันได้ในที่เดียว และแชร์ type/schema ร่วมกันได้โดยไม่ต้องเขียนซ้ำ

```
easyprint/
├── AGENTS.md              ← ไฟล์นี้ อ่านก่อนเริ่มงานเสมอ
├── docs/                   ← เอกสารประกอบ (proposal, ERD, API spec)
├── apps/
│   ├── web/                 ← หน้าเว็บที่ลูกค้า/ร้านค้า/แอดมินเห็น (React)
│   └── api/                  ← ระบบหลังบ้าน จัดการข้อมูล คำนวณราคา (ElysiaJS)
├── packages/
│   └── shared/                ← กฎการตรวจสอบข้อมูล (Zod) ที่ web และ api ใช้ร่วมกัน
└── .agents/skills/             ← ความรู้เฉพาะทางที่ AI agent โหลดเพิ่มเวลาต้องใช้
```

**ทำไมมี `packages/shared`:** ถ้าฝั่งเว็บกับฝั่งหลังบ้านต่างคนต่างเขียนกฎตรวจสอบข้อมูลเอง (เช่น "เบอร์โทรต้อง 10 หลัก") พอแก้กฎที่นึงแล้วลืมแก้อีกที่ ข้อมูลจะไม่ตรงกัน เก็บไว้ที่เดียวแล้วให้ทั้งคู่เรียกใช้ ปลอดภัยกว่า

---

## 4. Tech Stack และเหตุผลที่เลือก

| ชั้นงาน | เทคโนโลยี | ทำไมเลือก |
|---|---|---|
| Frontend | Next.js (App Router) | เฟรมเวิร์ก React ที่มีระบบ routing ในตัว (ไม่ต้องติดตั้ง React Router แยก) ตั้งชื่อโฟลเดอร์/ไฟล์ใน `app/` ก็กลายเป็นหน้าเว็บได้เลย และรองรับ Server Components ทำให้บางหน้าโหลดเร็วขึ้น |
| | TypeScript | ช่วยจับ error ตั้งแต่ตอนเขียนโค้ด ก่อนที่จะไปพังตอนรันจริง |
| | Tailwind CSS | จัดสไตล์หน้าเว็บโดยไม่ต้องเขียน CSS ไฟล์แยก ทำงานเร็วขึ้น |
| | shadcn/ui | มีปุ่ม, ฟอร์ม, ตาราง สำเร็จรูปสวยงาม ไม่ต้องออกแบบใหม่ตั้งแต่ศูนย์ |
| | Sarabun (Google Fonts) | ฟอนต์หลักของทั้งระบบ บังคับใช้ครบทุกหน้า ทุกองค์ประกอบ ทั้ง Frontend, Admin, และ Shop |
| Backend | Bun | ตัวรันโค้ด JavaScript ที่เร็วกว่า Node.js แบบเดิม |
| | ElysiaJS | เฟรมเวิร์กสร้าง API ที่เบาและเร็ว เหมาะกับ Bun |
| | Drizzle ORM | เขียนคำสั่งฐานข้อมูลด้วย TypeScript แทนเขียน SQL ตรงๆ ลดโอกาสพิมพ์ผิด |
| | Zod | ตรวจสอบว่าข้อมูลที่ส่งเข้ามาถูกต้องก่อนบันทึก (กัน error/ข้อมูลเพี้ยน) |
| | JWT + Argon2 | ระบบยืนยันตัวตนที่ปลอดภัย รหัสผ่านถูกเข้ารหัสก่อนเก็บเสมอ |
| Database | Supabase (PostgreSQL) | ฐานข้อมูลพร้อมใช้ ไม่ต้องติดตั้งเซิร์ฟเวอร์เอง มี dashboard ดูข้อมูลง่าย |
| Storage | Supabase Storage | เก็บไฟล์ที่ลูกค้าอัปโหลด (PDF, DOCX, รูปภาพ) แยกจากฐานข้อมูลหลัก |
| Deploy | GitHub | เก็บโค้ดและประวัติการแก้ไข (Version Control) |
| | Vercel | โฮสต์หน้าเว็บ (frontend) ให้โหลดเร็วทั่วโลก — Vercel เป็นบริษัทที่สร้าง Next.js เอง จึง deploy ได้ลื่นไหลไม่ต้องตั้งค่าอะไรเพิ่ม |
| | Render | โฮสต์ backend API |

---

## 5. คำสั่งที่ใช้บ่อย

```bash
bun install                          # ติดตั้ง library ทั้งหมดในโปรเจกต์ (ทำครั้งแรกหรือหลัง pull โค้ดใหม่)
bun --cwd apps/web dev               # เปิดหน้าเว็บโหมดพัฒนา (แก้โค้ดแล้วเห็นผลทันที)
bun --cwd apps/api dev               # เปิด backend โหมดพัฒนา
bun --cwd apps/api drizzle-kit push  # ส่งการเปลี่ยนแปลงโครงสร้างฐานข้อมูลขึ้น Supabase
```

---

## 6. กติกาการเขียนโค้ด (Coding Convention)

| กติกา | ทำไมต้องทำแบบนี้ |
|---|---|
| ทุก API endpoint ต้อง validate ด้วย Zod schema จาก `packages/shared` | กันไม่ให้ข้อมูลผิดรูปแบบเข้าไปในฐานข้อมูล เช่น ราคาติดลบ หรือเบอร์โทรไม่ครบหลัก |
| ห้ามเขียน type/interface ซ้ำระหว่าง frontend-backend | ถ้าแก้ทีเดียวที่ `packages/shared` ทั้งสองฝั่งอัปเดตตาม ไม่ต้องจำแก้ 2 ที่ |
| Password hash ด้วย Argon2 เท่านั้น ห้ามเก็บ plaintext | ถ้าฐานข้อมูลรั่วไหล รหัสผ่านผู้ใช้จะไม่ถูกอ่านได้ตรงๆ |
| JWT เก็บใน httpOnly cookie ไม่ใช่ localStorage | ป้องกันการขโมย token ผ่านสคริปต์อันตราย (XSS attack) |
| ชื่อไฟล์ component: PascalCase (เช่น `OrderCard.tsx`), ชื่อไฟล์ util/hook: camelCase (เช่น `useAuth.ts`) | ทีมงานอ่านโค้ดของกันและกันได้ง่ายขึ้น เดาได้ว่าไฟล์ไหนเป็นอะไร |

---

## 7. ⚠️ ข้อควรระวังเรื่อง Deployment (สำคัญมาก)

**Render free tier จะ "หลับ" backend หลังไม่มี request เข้า 15 นาที** และใช้เวลา "ตื่น" 30–60 วินาทีตอนมี request แรกเข้ามา

**ทำไมต้องรู้เรื่องนี้:** ถ้าไม่รู้ พอเจอเว็บโหลดช้าตอน demo หรือทดสอบ จะเข้าใจผิดว่าโค้ดมีบั๊ก แล้วเสียเวลาไล่หาปัญหาผิดจุด

**สิ่งที่ควรทำ:**
- ถ้ามีคน report ว่า "เว็บโหลดช้า/ค้าง" → **เช็คก่อนว่าเป็น cold start หรือไม่** โดยดูว่า request แรกช้ามากแต่ request ถัดไปเร็วปกติไหม ถ้าใช่ = ไม่ใช่บั๊ก เป็นเรื่องปกติของ hosting ฟรี
- ก่อนวันนำเสนอ/สอบโปรเจกต์ ให้ "ปลุก" backend ล่วงหน้าอย่างน้อย 2-3 นาที (เปิดเว็บทิ้งไว้เฉยๆ ก่อนเริ่มพรีเซนต์)
- ถ้าจำเป็นจริงๆ พิจารณาอัปเป็น Starter plan ($7/เดือน) ชั่วคราวเฉพาะช่วงนำเสนอ แล้วค่อยปิดทีหลัง
- **ห้ามพยายามแก้ปัญหานี้ด้วยการเปลี่ยนโค้ด backend** เพราะสาเหตุคือ hosting tier ไม่ใช่ตัวโค้ด

---

## 8. ห้ามทำ (Don'ts)

| ห้าม | เพราะอะไร |
|---|---|
| Commit ไฟล์ `.env` หรือ Supabase service key ขึ้น GitHub | ถ้าหลุดไปที่ repo สาธารณะ คนอื่นจะเข้าถึงฐานข้อมูล/ระบบได้ทันที |
| แก้ database schema ตรงๆ ใน Supabase dashboard | ทำให้ schema ในเครื่องกับบนคลาวด์ไม่ตรงกัน ต้องแก้ผ่าน Drizzle migration เท่านั้นเพื่อให้มีประวัติการเปลี่ยนแปลง ย้อนกลับได้ถ้าพัง |
| Push ตรงเข้า `main` branch | ถ้าโค้ดมีบั๊ก จะกระทบทุกคนในทีมทันที ให้ทำงานใน branch แยกแล้วเปิด PR ให้เพื่อนช่วยตรวจก่อน |

---

## 9. สำหรับเพื่อนร่วมทีมที่เพิ่งเข้ามาโปรเจกต์ครั้งแรก

ลำดับที่ควรทำ:
1. ติดตั้ง Antigravity แอป และ/หรือ Claude Code (ดูหัวข้อ 2)
2. Clone โปรเจกต์จาก GitHub มาไว้ในเครื่อง
3. รัน `bun install` ที่ root ของโปรเจกต์
4. ขอไฟล์ `.env` จากเพื่อนในทีม (ไม่ได้อยู่ใน GitHub เพราะเป็นข้อมูลลับ)
5. เปิด Antigravity แอป → เปิดโฟลเดอร์โปรเจกต์ → ลองพิมพ์ `agy` แล้วถามว่า "อ่าน AGENTS.md แล้วสรุปโปรเจกต์นี้ให้ฟังหน่อย" เพื่อเช็คว่า setup ถูกต้อง

---

## 10. เอกสารอ้างอิง

- `docs/proposal.md` — ข้อเสนอโครงการฉบับเต็ม (scope, tech stack, timeline)
- `docs/erd.md` — โครงสร้างฐานข้อมูล
- `docs/api-spec.md` — รายการ API endpoint
